# =====================================================

# APPLICATION

# =====================================================

spring.application.name=${SPRING_APPLICATION_NAME:backend_siop}
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

server.port=${SERVER_PORT:8080}

# =====================================================

<<<<<<< HEAD
# DATABASE - POSTGRESQL

# =====================================================

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER:org.postgresql.Driver}

# =====================================================

# JPA / HIBERNATE

# =====================================================
=======
spring.datasource.url=${SPRING_DATASOURCE_URL:}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver
>>>>>>> b64d078 (Fix: Remove hardcoded secrets and use environment variables)

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}

spring.jpa.properties.hibernate.dialect=${JPA_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:true}

spring.flyway.enabled=${FLYWAY_ENABLED:false}

# =====================================================

# JWT

# =====================================================

<<<<<<< HEAD
jwt.secret=${JWT_SECRET}
=======
jwt.secret=${JWT_SECRET:}
>>>>>>> b64d078 (Fix: Remove hardcoded secrets and use environment variables)
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# =====================================================

<<<<<<< HEAD
# MINIO
=======
minio.url=${MINIO_URL:http://localhost:9000}
minio.access-key=${MINIO_ACCESS_KEY:}
minio.secret-key=${MINIO_SECRET_KEY:}
minio.bucket-name=${MINIO_BUCKET_NAME:siop-fichiers}
minio.public-url=${MINIO_PUBLIC_URL:}

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
>>>>>>> b64d078 (Fix: Remove hardcoded secrets and use environment variables)

# =====================================================

minio.url=${MINIO_URL}
minio.access-key=${MINIO_ACCESS_KEY}
minio.secret-key=${MINIO_SECRET_KEY}
minio.bucket-name=${MINIO_BUCKET_NAME}
minio.public-url=${MINIO_PUBLIC_URL}

# =====================================================

# FILE UPLOAD

# =====================================================

spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:10MB}

# =====================================================

# MAIL / SMTP

# =====================================================

spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}

spring.mail.properties.mail.smtp.connectiontimeout=${MAIL_CONNECTION_TIMEOUT:5000}
spring.mail.properties.mail.smtp.timeout=${MAIL_TIMEOUT:5000}
spring.mail.properties.mail.smtp.writetimeout=${MAIL_WRITE_TIMEOUT:5000}

# =====================================================

<<<<<<< HEAD
# FRONTEND
=======
app.frontend-url=${APP_FRONTEND_URL:}
>>>>>>> b64d078 (Fix: Remove hardcoded secrets and use environment variables)

# =====================================================

app.frontend-url=${APP_FRONTEND_URL}

# =====================================================

<<<<<<< HEAD
# GROQ API
=======
integration.api-key=${INTEGRATION_API_KEY:}
integration.system.email=${INTEGRATION_SYSTEM_EMAIL:integration@spelev-system.local}
integration.fallback.technician-email=${INTEGRATION_FALLBACK_TECHNICIAN_EMAIL:integration-tech@spelev-system.local}
integration.fallback.technician-parc-name=${INTEGRATION_FALLBACK_TECHNICIAN_PARC_NAME:Parc URGENCE}
>>>>>>> b64d078 (Fix: Remove hardcoded secrets and use environment variables)

# =====================================================

groq.api-key=${GROQ_API_KEY}
groq.model=${GROQ_MODEL}
groq.base-url=${GROQ_BASE_URL}

# =====================================================

# INTEGRATION / N8N

# =====================================================

integration.api-key=${INTEGRATION_API_KEY}

integration.system.email=${INTEGRATION_SYSTEM_EMAIL}

integration.fallback.technician-email=${INTEGRATION_FALLBACK_TECHNICIAN_EMAIL}

integration.fallback.technician-parc-name=${INTEGRATION_FALLBACK_TECHNICIAN_PARC_NAME}

# =====================================================

# LOGGING

# =====================================================

logging.level.org.springframework.web=${LOG_LEVEL_WEB:INFO}

logging.level.org.springframework.security=${LOG_LEVEL_SECURITY:INFO}

logging.level.com.example.backend_siop=${LOG_LEVEL_APP:INFO}

logging.level.org.springframework.web.servlet.mvc=${LOG_LEVEL_MVC:INFO}

# =====================================================

# ENCODING

# =====================================================

server.servlet.encoding.charset=${SERVER_ENCODING_CHARSET:UTF-8}
server.servlet.encoding.enabled=${SERVER_ENCODING_ENABLED:true}
server.servlet.encoding.force=${SERVER_ENCODING_FORCE:true}
